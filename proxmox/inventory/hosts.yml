---
all:
  children:
    pve_nodes:
      hosts:
        # r720xd (192.168.11.15) was retired 2026-08-10. Hardware is gone; do not
        # re-add it. Its ZFS pools, NFS export and Sanoid datasets went with it.
        pve03:
          ansible_host: 192.168.11.12
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
    lxc_containers:
      hosts: {}
